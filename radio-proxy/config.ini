# Silver Line Radio Proxy — 설정 파일
# 환경변수(NSSM)가 있으면 환경변수가 우선합니다

# 로그 출력 (true/false)
# false 로 설정하면 로그 I/O 및 CPU 절약
LOG_ENABLED=true

# 오디오 설정
PORT=8088
# GAIN_DB: 하드웨어 GAIN 핀이 100kΩ→GND(6dB)일 때 3으로 설정
#          GAIN 핀이 100Ω→GND(3dB) 그대로라면 6 유지
GAIN_DB=3
